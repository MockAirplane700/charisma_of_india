import 'package:flutter/material.dart';


//color of the app bar
const Color primaryAppBarColor = Colors.deepPurple;

// color of the cards
const Color primaryCardColor = Colors.white10;

// color of the scaffold background
const Color primaryBackgroundColor = Colors.white;

// color of the buttons
const Color primaryButtonColor = Colors.blue;

// color of the drawer
const Color primaryDrawerColor = Colors.deepPurple;

// the generic snack bar
const SnackBar genericSnackBar = SnackBar(
    content: Text('Please note this application is a prototype for Charisma of india hence no payments can be made and all data present is tests data')
);